meta {
  name: create-thing-graphql
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/things
  body: graphql
  auth: none
}

body:graphql {
  mutation {
    addThing(name: "{{thingName}}") { id }
  }
}

body:graphql:vars {
  {
    "token": "{{secret:API_TOKEN}}",
    "tag": "{{tag}}"
  }
}
